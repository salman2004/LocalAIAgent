venv/
.venv/
__pycache__/
*.pyc

# Model weights are large and machine-specific — download separately per README
models/

# Local vector DB contents (regenerate via scripts/ingest_docs.py)
rag_store/lancedb/

# llama.cpp/whisper.cpp/Piper binaries fetched by scripts/setup_*.ps1
vendor/

# Runtime state (e.g. the ask_claude persistent session id) - machine-specific, not source
.state/

.env
